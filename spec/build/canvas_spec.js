// Generated by CoffeeScript 1.8.0
(function() {
  Kona.ready(function() {
    var canvasScene;
    Kona.Canvas.init('canvas');
    canvasScene = new Kona.Scene({
      name: 'canvasScene',
      active: true
    });
    Kona.Engine.start();
    return describe("Kona.Canvas", function() {
      return describe("::init", function() {
        it("throws an exception if DOM element not found", function() {
          return expect(function() {
            return Kona.Canvas.init("fake");
          }).toThrow();
        });
        return it("sets default dimensions", function() {
          var elem;
          elem = Kona.Canvas.elem;
          expect(elem.getAttribute('width')).toBe('720');
          return expect(elem.getAttribute('height')).toBe('540');
        });
      });
    });
  });

}).call(this);
