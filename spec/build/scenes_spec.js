// Generated by CoffeeScript 1.3.3
(function() {

  Kona.ready(function() {
    var main;
    Kona.Canvas.init('canvas');
    main = new Kona.Scene({
      name: 'main',
      active: true
    });
    Kona.Engine.start();
    return describe("Kona.Scenes", function() {
      return describe("::find", function() {
        it("finds a scene by name", function() {
          var scene;
          scene = Kona.Scenes.find("main");
          return expect(scene).toEqual(main);
        });
        return it("returns null for scenes not found", function() {
          var scene;
          scene = Kona.Scenes.find("fake");
          return expect(scene).toBe(null);
        });
      });
    });
  });

  describe("Kona.Scene", function() {});

}).call(this);
