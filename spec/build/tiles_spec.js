// Generated by CoffeeScript 1.3.3
(function() {

  Kona.ready(function() {
    describe("Kona.Tile", function() {
      it("defines a tileSize", function() {
        return expect(Kona.Tile.tileSize).toBe(60);
      });
      it("sets a default group", function() {
        var tile;
        tile = new Kona.Tile;
        return expect(tile.group).toBe('tiles');
      });
      return it("has correct dimensions", function() {
        var tile;
        tile = new Kona.Tile;
        expect(tile.box.width).toBe(Kona.Tile.tileSize);
        return expect(tile.box.height).toBe(Kona.Tile.tileSize);
      });
    });
    return describe("Kona.BlankTile", function() {
      return it("is not solid", function() {
        var tile;
        tile = new Kona.BlankTile;
        return expect(tile.solid).toBe(false);
      });
    });
  });

}).call(this);
